function plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N function plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
end
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endimport java.util.*;

public class Compute
{
	public static void main(String[] args)
	{
		function(10);
	}
	public static double function(int j)
	{
		double x = 0;
		for (int i = j * (-1); i < j; i++) {
			if (i == 0) System.out.println("f(x) is undefined");
			else x = j; 
		double numerator = (5 * (x * x) - (2 * x) + 4) / (1 / x);
		double denominator = (7 * x + 3) / (1 / x);
		double result = (((5 * x ) - 2 + (4 / x) ) / ( 7 + (3 / x);  // num divided by denom
		int limit = 0;
		if (result == 0) limit = i; }
		System.out.println("Limit is " + i);
	}
}
function plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
endfunction plot_bigauss1(N)
  
  A = randn([2 2]);
  S = A*A'
  cS = chol(S);
  
  X = randn([2 N]);
  Y = cS'*X;
  
  plot(X(1,:), X(2,:), 'o');
  xlim([-6 6]);
  ylim([-6 6]);
 
  pause;

  hold on;
  plot([X(1,:) ; Y(1,:)], [X(2,:) ; Y(2,:)], 'k-');
  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
  hold off;
  
  pause;

  plot(Y(1,:), Y(2,:), 'rx');
  xlim([-6 6]);
  ylim([-6 6]);
  
end