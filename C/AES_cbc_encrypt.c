#include <stdio.h>

void AES_cbc_encrypt(constunsigned char *in,
		unsigned char*out,
		size_t length,
		constAES_KEY*key,
		unsigned char*ivec,
		AES_ENCRYPT);
int main(void)
{

}

void AES_cbc_encrypt(const unsigned char *in,
		     unsigned char *out,
		     size_t length,
		     const AES_KEY *key,
		     unsignd char *ivec,
		     AES_ENCRYPT or AES_DECRYPT) {

}
    
